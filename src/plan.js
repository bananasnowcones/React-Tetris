// 1. Make board [DONE]
    // a. Stage, Board, Displays, Cells, Start Button, Instruction Button
    // b. Styling too
    // c. Create Stage function
// 2. Make tetrominos [DONE]
    // a. Randomizer for pieces
    // b. Colors
// 3. Hooks
    // a. Keep track of player space (position, shape, collision flag) 
    // b. Keep track of board render (board, rows cleared, collisions)
    // c. Keep track of lines cleared/score/game status (score, rows, level, )
    // d/ Keep track of inverval (dropTime)
// 4. Movement (Left, Right, Drop)
    // a. Keep track of player space
    // b. Collision Detection
// 5. Rotation
// 6. Drop (Game Loop)
    // a. Keep track of board renders
// 7. Clear Rows
// 8. Check Game Status
// 9. Holding
// 10. Show next block
// 11. Fast Drop
// 12. Music